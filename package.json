{
  "name": "app",
  "version": "1.0.0",
  "description": "A node app without any 3rd party lib",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch index.js --watch lib  --exec 'npm start'",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stanislas Ormières",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.18.2"
  }
}
